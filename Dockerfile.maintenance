# Mantenimiento — NO hace build de React, sirve solo la página estática
FROM nginx:alpine
COPY maintenance.html /usr/share/nginx/html/index.html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
